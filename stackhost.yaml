user:
  name: cevilion 
  github: incredaimaso

language: "python"
python_version: "3.10"

commands:
  build:
    - "apt-get -qq install ffmpeg"
       "pip3 install -r requirements.txt"
  package:
    - "nano inxi"
  start:
    - "python3 -m bot"

env:
  - "ENVIRONMENT=production"
