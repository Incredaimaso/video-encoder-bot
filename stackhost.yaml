user:
  name: cevilion 
  github: incredaimaso

language: "python"

commands:
  build:
    - "pip3 install -r requirements.txt"
  package:
    - "nano inxi"
    - "apt-get -qq install ffmpeg"
  start:
    - "python3 -m bot"

env:
  - "ENVIRONMENT=production"
